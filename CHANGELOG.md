# 更新日志（Changelog）

本项目的所有重要变更都记录在此文件中，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [0.2.1] - 2026-09-17

### 修复
- 修复控制端「远程操作网页」预览黑屏：播放端预览截图改用 WebView2 `CapturePreviewAsync` 直接抓取 Chromium 渲染表面（`RenderTargetBitmap` 无法捕获 WebView2 独立子窗口，网页画面此前始终为黑）。

### 新增
- 应用图标：蓝色圆角底 + 白色显示器 + 播放三角 + 投屏波纹，集成到 exe 与所有窗口。

## [0.2.0] - 2026-09-14

### 新增
- **设备备注**：控制端可为每台播放设备添加、修改备注，备注显示在设备列表中，用于识别设备对应的播放屏（持久化到 `console.json`）。
- **网页远程操作**：控制端可实时操作播放端正在显示的网页，支持：
  - 点击画面（关闭广告、点击按钮等）；
  - 输入文本（登录表单等）；
  - 回车提交、退格；
  - 刷新页面、前进、后退；
  - 执行自定义 JavaScript。

### 变更
- **移除播放端右下角时间水印**：删除时钟显示及相关设置项。

### 修复
- 移除 `app.manifest` 中显式声明的 `PerMonitorV2`（会导致 .NET 10 WPF 在 Windows 11 启动即崩溃，异常码 `0xC0000409`）。
- 实时预览改为 WPF 渲染管线抓屏（`RenderTargetBitmap`），多显示器枚举改用 Win32 P/Invoke（`EnumDisplayMonitors`），移除对 WinForms 的依赖。
- 修复设备唯一 ID 每次启动重新生成的问题（现首次生成后持久化，控制台可稳定识别设备）。

## [0.1.0] - 2026-09-11

### 新增
- 首个可用版本，完成 M0–M5 全部里程碑：
  - 单 EXE 双角色（播放端 / 控制台）；
  - 播放端：全屏无边框、图片/视频/网页混合轮播、本地离线缓存续播、开机自启、实时预览回传、紧急消息插播、定时开关屏、崩溃自动重启；
  - 控制台：设备自动发现、实时缩略预览、素材库、节目单编排、一键下发、多节目单、远程控制、设备分组、密码锁定；
  - 网络：UDP 广播发现 + TCP 帧协议（端口 8790/8791/8792）。
